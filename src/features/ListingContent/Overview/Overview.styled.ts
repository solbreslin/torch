import styled from 'styled-components';

export const OverviewStyled = styled.div`
  display: flex;
  gap: 1.75rem;

  > span {
    align-items: center;
    display: flex;
    gap: 0.4em;

    svg {
      fill: currentColor;
    }
  }
`;
